#pragma once
// GENERATED by CMake (cmake/DetectISA.cmake + simd_config.hpp.in) — do not edit.
// Records what the build system detected about THIS build's target ISA.

namespace swaps::simd::detected {

// Human-readable ISA level, e.g. "AVX2+FMA", "AVX-512", "NEON".
inline constexpr const char* isa_name = "@SWAPS_ISA_NAME@";

// Architecture flags this translation unit was compiled with.
inline constexpr const char* arch_flags = "@SWAPS_ARCH_FLAGS@";

// Doubles per SIMD register, as detected by CMake. Cross-checked against
// Eigen's packet_traits<double>::size in <swaps/simd.hpp>.
inline constexpr int double_packet_size = @SWAPS_DOUBLE_PACKET_SIZE@;

// Target triple / processor, for the benchmark fingerprint.
inline constexpr const char* target_processor = "@CMAKE_SYSTEM_PROCESSOR@";
inline constexpr const char* compiler_id      = "@CMAKE_CXX_COMPILER_ID@";
inline constexpr const char* compiler_version = "@CMAKE_CXX_COMPILER_VERSION@";

}  // namespace swaps::simd::detected
